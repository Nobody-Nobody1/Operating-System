{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run My Batch File",
            "request": "launch",
            "program": "cmd.exe",            // Windows Command Prompt
            "args": ["/c", "${workspaceFolder}\\scripts\\myfile.bat"], 
            "cwd": "${workspaceFolder}",     // Working directory
            "console": "integratedTerminal", // Show output in VS Code terminal
        }
    ]
}
